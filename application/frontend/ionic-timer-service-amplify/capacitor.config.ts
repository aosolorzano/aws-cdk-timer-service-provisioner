import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-timer-service-amplify',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
